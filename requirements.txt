# RVC Song Processor - Manual Dependencies Only
# Auto-installed by gradio/fairseq: pydantic, requests, tqdm, joblib, pooch, decorator, lazy_loader, msgpack, packaging

# UI
gradio==5.42.0

# RVC
fairseq==0.12.2
praat-parselmouth==0.4.6
pyworld==0.3.4
torchcrepe==0.0.24
faiss-cpu==1.7.3

# Audio separator deps (must install manually)
beartype==0.18.5
diffq-fixed==0.2.4
einops==0.8.1
julius==0.2.7
ml_collections
onnx-weekly
onnx2torch-py313
rotary-embedding-torch==0.6.5
samplerate==0.1.0

# Utils
yt-dlp
ffmpeg-python==0.2.0
imageio-ffmpeg
